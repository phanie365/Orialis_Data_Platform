{
  "name": "orialis-erp-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal internal Finance/Operations frontend for the Orialis ERP API. Read-only; reaches the API only through a server-side proxy that holds the key.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/*.test.js\""
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
